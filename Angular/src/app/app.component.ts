import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  // using inline styling instead of separtate stylesheet
  styles: [] 
})
export class AppComponent {
  title = 'Angular';
}
